# CLAUDE.md

Read [AGENTS.md](./AGENTS.md). It holds the project context (stack, scripts, env vars, deploy notes, gotchas, next steps) and the TanStack Intent skill mappings, all in one file. This file exists so Claude Code picks up the same guidance — no separate content lives here.
